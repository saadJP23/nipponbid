apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: nipponbid
  labels:
    app: mysql
spec:
  type: ClusterIP
  selector:
    app: mysql
    statefulset.kubernetes.io/pod-name: mysql-0
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
