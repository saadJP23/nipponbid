
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: nipponbid
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
